# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
npm run install-all
```

Это установит зависимости для сервера и клиента.

## Шаг 2: Настройка Email

### Создайте файл .env

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

### Получите пароль приложения Mail.ru

1. Откройте: https://account.mail.ru/user/2-step-auth/passwords/
2. Включите двухфакторную аутентификацию (если не включена)
3. Нажмите "Создать пароль"
4. Введите название: "Usmanka Weekend"
5. Скопируйте пароль

### Обновите .env файл

Откройте `.env` и замените:

```env
EMAIL_PASS=ваш_пароль_приложения_здесь
```

На полученный пароль (без пробелов).

## Шаг 3: Тест Email

```bash
npm run test-email
```

Если видите "✅ Email отправлен успешно!" - всё настроено правильно!

## Шаг 4: Запуск проекта

```bash
npm run dev
```

Это запустит:
- 🖥️ Backend сервер на http://localhost:5000
- 🌐 React приложение на http://localhost:3000

Откройте http://localhost:3000 в браузере.

## Шаг 5: Тестирование бронирования

1. Откройте любой тур
2. Выберите тариф
3. Нажмите "Забронировать"
4. Заполните форму
5. Нажмите "Отправить заявку"

✅ Вы должны получить email на antonnechlopochin@mail.ru

## Возможные проблемы

### "EMAIL_PASS не установлен"

Создайте файл `.env` и добавьте пароль приложения.

### "Invalid login"

Проверьте:
- Правильность email: antonnechlopochin@mail.ru
- Используете пароль приложения (не обычный пароль)
- Двухфакторная аутентификация включена

### "Port 3000 already in use"

Другое приложение использует порт 3000. Остановите его или измените порт:

```bash
# Windows
set PORT=3001 && npm run client

# Linux/Mac
PORT=3001 npm run client
```

### "Port 5000 already in use"

Измените порт в `.env`:

```env
PORT=5001
```

## Готово! 🎉

Теперь сайт работает и отправляет уведомления о бронированиях на email.

## Следующие шаги

1. Замените placeholder изображения на реальные фото
2. Обновите контактные данные в `config.py`
3. Настройте интеграцию с ЮKassa для приема платежей
4. Разверните на сервере (см. DEPLOYMENT.md)

## Полезные команды

```bash
# Запуск в режиме разработки
npm run dev

# Только сервер
npm run server

# Только клиент
npm run client

# Тест email
npm run test-email

# Сборка для продакшена
npm run build

# Запуск продакшен версии
npm start
```

## Нужна помощь?

Смотрите подробные инструкции:
- 📧 EMAIL_SETUP.md - настройка email
- 🚀 DEPLOYMENT.md - развертывание на сервере
- 📚 README_REACT.md - полная документация
