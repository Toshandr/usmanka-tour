# Усманка-уикенд - React + Node.js версия

Современный лендинг для активных туров выходного дня на природе Воронежской области.

## 🚀 Технологии

### Frontend
- **React 18** - UI библиотека
- **React Router** - маршрутизация
- **Axios** - HTTP клиент
- **CSS3** - стилизация

### Backend
- **Node.js** - серверная платформа
- **Express** - веб-фреймворк
- **CORS** - кросс-доменные запросы
- **Nodemailer** - отправка email

## 📦 Установка

### Предварительные требования
- Node.js 16+ и npm
- Git

### Шаг 1: Клонирование репозитория

```bash
git clone <your-repo-url>
cd usmanka-weekend
```

### Шаг 2: Установка зависимостей

```bash
# Установка зависимостей для сервера и клиента
npm run install-all
```

Или вручную:

```bash
# Установка зависимостей сервера
npm install

# Установка зависимостей клиента
cd client
npm install
cd ..
```

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
# Запуск сервера и клиента одновременно
npm run dev
```

Это запустит:
- Backend сервер на http://localhost:5000
- React приложение на http://localhost:3000

### Запуск по отдельности

```bash
# Только сервер
npm run server

# Только клиент
npm run client
```

## 🏗️ Сборка для продакшена

```bash
# Сборка React приложения
npm run build
```

Это создаст оптимизированную версию в папке `client/build`

## 🚀 Развертывание

### Вариант 1: Heroku

```bash
# Установите Heroku CLI
# Создайте приложение
heroku create usmanka-weekend

# Деплой
git push heroku main
```

### Вариант 2: VPS/VDS

```bash
# На сервере
git clone <your-repo-url>
cd usmanka-weekend

# Установка зависимостей
npm run install-all

# Сборка клиента
npm run build

# Установка PM2
npm install -g pm2

# Запуск с PM2
pm2 start server/index.js --name usmanka-weekend
pm2 save
pm2 startup
```

### Настройка Nginx

```nginx
server {
    listen 80;
    server_name your-domain.ru;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 📁 Структура проекта

```
usmanka-weekend/
├── client/                 # React приложение
│   ├── public/            # Статические файлы
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── pages/         # Страницы
│   │   ├── App.js         # Главный компонент
│   │   └── index.js       # Точка входа
│   └── package.json
├── server/                # Node.js сервер
│   ├── data/             # Данные туров
│   └── index.js          # Express сервер
├── package.json          # Зависимости сервера
└── README_REACT.md       # Этот файл
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
PORT=5000
NODE_ENV=production

# Email настройки
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-password

# ЮKassa
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
```

## 📝 API Endpoints

### GET /api/health
Проверка работоспособности сервера

### GET /api/tours
Получение всех туров

### GET /api/tours/:tourId
Получение конкретного тура

### POST /api/booking
Создание заявки на бронирование

**Body:**
```json
{
  "name": "Имя",
  "phone": "+7 (900) 123-45-67",
  "email": "email@example.com",
  "tour": "dance",
  "tariff": "Мини с подселением",
  "amount": "6000"
}
```

## 🎨 Особенности дизайна

- ✅ Адаптивный дизайн для всех устройств
- ✅ Темная тема с акцентами
- ✅ Плавные анимации и переходы
- ✅ Оптимизирован для аудитории 18-60 лет
- ✅ Доступность (a11y)
- ✅ SEO оптимизация

## 🔒 Безопасность

- CORS настроен для безопасных запросов
- Валидация данных на сервере
- Защита от XSS атак
- HTTPS ready

## 📱 Поддержка браузеров

- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE

## 📞 Контакты

- Email: info@usmanka-weekend.ru
- Телефон: +7 (900) 123-45-67
- Адрес: г. Воронеж, пл. Ленина

---

Сделано с ❤️ для любителей активного отдыха
