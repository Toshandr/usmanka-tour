# Руководство по миграции с Python/FastAPI на React/Node.js

## 📋 Обзор изменений

Проект был полностью переписан с Python (FastAPI) на современный стек React + Node.js для улучшения производительности, масштабируемости и удобства разработки.

## 🔄 Что изменилось

### Backend
- **Было:** Python + FastAPI + Jinja2
- **Стало:** Node.js + Express + REST API

### Frontend
- **Было:** Server-side rendering с Jinja2 шаблонами
- **Стало:** React SPA (Single Page Application)

### Преимущества новой версии
✅ Более быстрая разработка с hot-reload
✅ Лучшая производительность на клиенте
✅ Современный UI/UX с React
✅ Легче масштабировать
✅ Больше возможностей для интерактивности
✅ Единый язык (JavaScript) на фронте и бэке

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установка всех зависимостей
npm run install-all
```

### 2. Настройка окружения

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env файл
nano .env
```

### 3. Запуск в режиме разработки

```bash
# Запуск сервера и клиента одновременно
npm run dev
```

Откройте http://localhost:3000 в браузере

## 📁 Сравнение структуры

### Старая структура (Python)
```
├── main.py                 # FastAPI сервер
├── templates/              # Jinja2 шаблоны
│   ├── index.html
│   ├── tour.html
│   └── ...
├── static/                 # Статические файлы
│   ├── css/
│   └── js/
├── data/
│   └── tours.py           # Данные туров
└── requirements.txt
```

### Новая структура (React + Node.js)
```
├── server/                 # Node.js backend
│   ├── index.js           # Express сервер
│   └── data/
│       └── tours.js       # Данные туров
├── client/                # React frontend
│   ├── public/            # Статические файлы
│   └── src/
│       ├── components/    # React компоненты
│       ├── pages/         # Страницы
│       ├── App.js
│       └── index.js
├── package.json           # Зависимости сервера
└── .env                   # Конфигурация
```

## 🔧 Миграция данных

Данные туров были автоматически конвертированы из Python в JavaScript формат:

**Было (Python):**
```python
tours_data = {
    'dance': {
        'title': 'Танцевальный девичник',
        'price': 6000,
        # ...
    }
}
```

**Стало (JavaScript):**
```javascript
const toursData = {
  dance: {
    id: 'dance',
    title: 'Танцевальный девичник',
    price: 6000,
    // ...
  }
};
```

## 🎨 Изменения в дизайне

### Единый стиль
- ✅ Меню теперь в едином стиле с остальным сайтом (glass эффект)
- ✅ Все страницы используют единую цветовую схему
- ✅ Улучшена читаемость для аудитории 18-60 лет
- ✅ Адаптивный дизайн для всех устройств

### Цветовая палитра
- Основной фон: `#0f172a` (темно-синий)
- Акцент: `#4CAF50` (зеленый)
- Вторичный акцент: `#FF9800` (оранжевый)
- Текст: `#f1f5f9` (светло-серый)

## 📡 API Endpoints

### Старые маршруты (FastAPI)
```
GET  /                      → Главная страница
GET  /tour/{tour_key}       → Страница тура
POST /api/booking           → Бронирование
```

### Новые маршруты (Express)
```
GET  /api/tours             → Получить все туры (JSON)
GET  /api/tours/:tourId     → Получить тур (JSON)
POST /api/booking           → Создать заявку (JSON)
GET  /api/health            → Проверка здоровья
```

React Router обрабатывает клиентскую маршрутизацию:
```
/                           → Главная страница
/tour/:tourId               → Страница тура
```

## 🔐 Безопасность

### Что осталось
- ✅ Валидация данных
- ✅ CORS защита
- ✅ Безопасное хранение ключей API

### Что добавлено
- ✅ Helmet.js для HTTP заголовков (рекомендуется)
- ✅ Rate limiting (рекомендуется)
- ✅ JWT токены для аутентификации (если нужно)

## 📦 Развертывание

### Старый способ (Python)
```bash
pip install -r requirements.txt
python main.py
```

### Новый способ (Node.js)
```bash
npm install
npm run build
npm start
```

## 🐛 Известные проблемы и решения

### Проблема: Порт уже занят
```bash
# Измените порт в .env
PORT=5001
```

### Проблема: CORS ошибки
```bash
# Убедитесь, что в server/index.js настроен CORS
app.use(cors());
```

### Проблема: React не подключается к API
```bash
# Проверьте proxy в client/package.json
"proxy": "http://localhost:5000"
```

## 📚 Дополнительные ресурсы

- [React документация](https://react.dev/)
- [Express документация](https://expressjs.com/)
- [Node.js документация](https://nodejs.org/)
- [React Router](https://reactrouter.com/)

## ✅ Чеклист миграции

- [ ] Установлены Node.js и npm
- [ ] Установлены все зависимости (`npm run install-all`)
- [ ] Создан и настроен .env файл
- [ ] Проект запускается локально (`npm run dev`)
- [ ] Все туры отображаются корректно
- [ ] Форма бронирования работает
- [ ] Дизайн соответствует требованиям
- [ ] Сайт адаптивен на мобильных устройствах
- [ ] Настроен production build
- [ ] Проект развернут на сервере

## 🆘 Поддержка

Если возникли проблемы при миграции:

1. Проверьте версии Node.js (`node -v`) и npm (`npm -v`)
2. Удалите node_modules и переустановите: `rm -rf node_modules && npm install`
3. Проверьте логи: `npm run dev` покажет ошибки
4. Убедитесь, что порты 3000 и 5000 свободны

---

**Важно:** Старая Python версия сохранена в проекте для справки. Все файлы Python можно удалить после успешной миграции.
