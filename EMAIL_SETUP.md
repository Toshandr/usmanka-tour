# Настройка отправки Email уведомлений

## Для Mail.ru (antonnechlopochin@mail.ru)

### Шаг 1: Получение пароля приложения

1. Войдите в свой аккаунт Mail.ru
2. Перейдите в настройки: https://account.mail.ru/user/2-step-auth/passwords/
3. Включите двухфакторную аутентификацию (если еще не включена)
4. Создайте "Пароль для внешнего приложения"
5. Скопируйте полученный пароль

### Шаг 2: Настройка .env файла

Создайте файл `.env` в корне проекта:

```env
# Server
PORT=5000
NODE_ENV=development

# Email для Mail.ru
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=antonnechlopochin@mail.ru
EMAIL_PASS=ваш_пароль_приложения_здесь

# Контакты
CONTACT_PHONE=+7 (900) 123-45-67
CONTACT_EMAIL=info@usmanka-weekend.ru
CONTACT_ADDRESS=г. Воронеж, пл. Ленина
```

### Шаг 3: Проверка работы

После настройки запустите сервер:

```bash
npm run server
```

Сделайте тестовое бронирование через сайт. Вы должны получить email на antonnechlopochin@mail.ru

## Альтернатива: Gmail

Если хотите использовать Gmail вместо Mail.ru:

### Шаг 1: Включите двухфакторную аутентификацию

1. Перейдите: https://myaccount.google.com/security
2. Включите "Двухэтапная аутентификация"

### Шаг 2: Создайте пароль приложения

1. Перейдите: https://myaccount.google.com/apppasswords
2. Выберите "Почта" и "Другое устройство"
3. Введите название: "Usmanka Weekend"
4. Скопируйте 16-значный пароль

### Шаг 3: Обновите .env

```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=ваш_16_значный_пароль
```

## Формат получаемого письма

При каждом бронировании на почту antonnechlopochin@mail.ru придет красиво оформленное письмо с:

- 📋 Информацией о клиенте (имя, телефон, email)
- 🎫 Деталями бронирования (тур, тариф, стоимость)
- ⏰ Напоминанием связаться с клиентом
- 📅 Датой и временем получения заявки

## Тестирование

### Тест отправки email через код

Создайте файл `test-email.js` в корне проекта:

```javascript
require('dotenv').config();
const nodemailer = require('nodemailer');

const transporter = nodemailer.createTransport({
  host: 'smtp.mail.ru',
  port: 465,
  secure: true,
  auth: {
    user: 'antonnechlopochin@mail.ru',
    pass: process.env.EMAIL_PASS
  }
});

transporter.sendMail({
  from: '"Усманка-уикенд" <antonnechlopochin@mail.ru>',
  to: 'antonnechlopochin@mail.ru',
  subject: 'Тест отправки email',
  html: '<h1>Тест успешен!</h1><p>Email настроен правильно.</p>'
}).then(() => {
  console.log('✅ Email отправлен успешно!');
}).catch((error) => {
  console.error('❌ Ошибка:', error);
});
```

Запустите:

```bash
node test-email.js
```

## Возможные проблемы

### Ошибка: "Invalid login"

- Проверьте правильность email и пароля
- Убедитесь, что используете пароль приложения, а не обычный пароль
- Проверьте, что двухфакторная аутентификация включена

### Ошибка: "Connection timeout"

- Проверьте настройки порта (465 для Mail.ru)
- Убедитесь, что нет блокировки файрволом
- Попробуйте использовать другой порт (587 с secure: false)

### Письма не приходят

- Проверьте папку "Спам"
- Убедитесь, что сервер запущен
- Проверьте логи сервера на наличие ошибок

## Безопасность

⚠️ **ВАЖНО:**

1. Никогда не коммитьте файл `.env` в Git
2. Используйте только пароли приложений, не основные пароли
3. Регулярно меняйте пароли приложений
4. Добавьте `.env` в `.gitignore` (уже добавлено)

## Дополнительные настройки

### Отправка копии клиенту

Если хотите, чтобы клиент тоже получал подтверждение, раскомментируйте в `server/index.js`:

```javascript
// Отправка подтверждения клиенту
await transporter.sendMail({
  from: '"Усманка-уикенд" <antonnechlopochin@mail.ru>',
  to: email, // email клиента
  subject: 'Ваша заявка принята - Усманка-уикенд',
  html: `<h1>Спасибо за заявку!</h1><p>Мы свяжемся с вами в ближайшее время.</p>`
});
```

### Изменение получателя

Чтобы отправлять на другой email, измените в `server/index.js`:

```javascript
to: 'antonnechlopochin@mail.ru', // Замените на нужный email
```

## Поддержка

Если возникли проблемы с настройкой email:

1. Проверьте логи сервера: `npm run server`
2. Запустите тест: `node test-email.js`
3. Проверьте настройки в `.env` файле
4. Убедитесь, что все зависимости установлены: `npm install`
