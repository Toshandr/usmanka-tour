# Гайд по деплою на Netlify

## Описание

Этот проект готов к статическому деплою на Netlify. React приложение будет скомпилировано в статические файлы (HTML, CSS, JS) и развёрнуто на CDN.

## Предварительные требования

- GitHub аккаунт
- Netlify аккаунт (регистрация через GitHub)
- Git для управления версиями

## Шаг 1: Подготовка репозитория

Убедитесь, что ваш репозиторий на GitHub содержит эти файлы:

```
usmanka-tour/
├── netlify.toml          ← Конфигурация Netlify
├── .netlifyignore        ← Какие файлы игнорировать
├── client/               ← React приложение
│   ├── public/
│   ├── src/
│   └── package.json
└── package.json
```

## Шаг 2: Подключение к Netlify

### Способ 1: Через Netlify UI (рекомендуется)

1. Перейдите на [netlify.com](https://netlify.com) и войдите в аккаунт
2. Нажмите **"New site from Git"**
3. Выберите **GitHub** и авторизуйте доступ
4. Выберите репозиторий `usmanka-tour`
5. На странице **Build settings** проверьте:
   - **Owner**: ваш GitHub аккаунт
   - **Branch to deploy**: `main`
   - **Build command**: оставить по умолчанию (Netlify автоматически прочитает `netlify.toml`)
   - **Publish directory**: `client/build`
6. Нажмите **Deploy site**

### Способ 2: Через Netlify CLI

```bash
# Установка Netlify CLI
npm install -g netlify-cli

# Вход в Netlify
netlify login

# Инициализация проекта
netlify init

# Деплой
netlify deploy --prod
```

## Шаг 3: Проверка конфигурации

Netlify автоматически прочитает `netlify.toml` с следующими параметрами:

- **Build command**: `cd client && npm run build`
- **Publish directory**: `client/build`
- **Node version**: `18.17.0`

## Шаг 4: Переменные окружения (если требуются)

Если в будущем нужны переменные окружения для контактной информации:

1. В Netlify UI: **Site settings** → **Build & deploy** → **Environment**
2. Добавьте переменные:
   ```
   REACT_APP_CONTACT_EMAIL=your-email@example.com
   REACT_APP_CONTACT_PHONE=+7-900-000-0000
   ```

## Шаг 5: После деплоя

После успешного деплоя вы получите:

✅ Публичный URL сайта: `https://your-site-name.netlify.app`
✅ Автоматический деплой при каждом push в main ветку
✅ SSL сертификат (HTTPS по умолчанию)
✅ CDN и кэширование

## Оптимизация

### Кэширование

Конфигурация `netlify.toml` включает оптимальные заголовки кэширования:

- **HTML файлы**: кэш на 0 (всегда свежие)
- **JS/CSS бундлы**: кэш на 1 год (с fingerprinting React)
- **Статические файлы**: кэш на 1 год

### Маршрутизация

Все запросы к несуществующим путям автоматически перенаправляются на `index.html` для корректной работы React Router.

## Решение проблем

### Ошибка "Build failed"

1. Проверьте логи деплоя в Netlify UI
2. Убедитесь, что `client/package.json` существует
3. Проверьте, что все зависимости установлены

### Белый экран при открытии сайта

1. Откройте DevTools (F12) и проверьте консоль на ошибки
2. Проверьте, что React Router правильно настроен
3. Проверьте пути к assets (должны быть относительные)

### Ошибка 404 на вложенных маршрутах

Это уже решено в `netlify.toml` редиректом на `index.html`. Если проблема остаётся:

1. Убедитесь, что `netlify.toml` в корне проекта
2. Пересоздайте сайт на Netlify (Site settings → Danger zone → Delete this site)

## Локальное тестирование перед деплоем

```bash
# Установка зависимостей
npm run install-all

# Локальная сборка
npm run build

# Локальное тестирование сборки
cd client && npm install -g serve && serve -s build
```

Откройте http://localhost:3000 и проверьте функциональность.

## Полезные команды

```bash
# Просмотр статуса деплоя
netlify status

# Просмотр логов
netlify log

# Откат к предыдущему деплою
netlify api sites list  # получить site ID
netlify api deploys list --site [SITE_ID]
```

## Документация

- [Netlify Docs](https://docs.netlify.com)
- [React Deployment](https://create-react-app.dev/docs/deployment)
- [Netlify TOML Reference](https://docs.netlify.com/configure-builds/file-conventions/#netlify-toml)

---

**Успешного деплоя! 🚀**
